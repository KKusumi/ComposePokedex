import dependencies.Dep

apply from: "${rootDir.absolutePath}/android_common_build.gradle"

dependencies {
    implementation project(':model')
    implementation project(':data:repository')

    // KotlinResult
    implementation Dep.Other.kotlinResult
}